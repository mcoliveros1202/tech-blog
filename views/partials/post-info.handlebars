<article class="post">
      <div class="title">
        <a href="/post/{{id}}" target="_blank">{{title}}</a>
        <span>{{content}}</span>
      </div>
      <div class="meta">
       Created by {{user.username}} on {{create_at}}|
        <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
      </div>
    </article>